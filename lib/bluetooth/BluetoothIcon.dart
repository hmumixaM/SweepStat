import 'package:flutter/material.dart';

class BluetoothIcon extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return IconButton(onPressed: () {}, icon: Icon(Icons.bluetooth_disabled));
  }
}
